{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "statusLine": {
    "type": "command",
    "command": "bash -c 'command -v ccusage >/dev/null 2>&1 && ccusage statusline || printf \"⚠ no ccusage — install with: npm i -g ccusage (keeps Pro usage visible)\"'"
  }
}
